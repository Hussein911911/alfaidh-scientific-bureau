# 🔄 سير العمل الاحترافي (Development Workflow)

## كيف نشتغل:

```
أنت ──(طلب)──> أنا ──(تعديل)──> GitHub ──(تلقائي)──> Vercel ──(تلقائي)──> المنصة
```

## الخطوات:

### 1. الطلب
- تخبرني شنو تبي نضيف أو نعدل
- ممكن تكون ميزة جديدة، تعديل، أو إصلاح خطأ

### 2. التعديل
- أعدل الكود في المشروع
- أختبره محلياً
- أضيف commit واضح

### 3. الرفع
- أرفع على GitHub تلقائياً
- الـ commit يحتوي على وصف واضح

### 4. النشر التلقائي
- Vercel يسوي Deploy تلقائي عند أي push
- ما تحتاج تسوي شي!

### 5. التجربة
- تروح للموقع وتجرّب الميزة الجديدة

## نصائح مهمة:

1. **صفّح المنصة** (Ctrl+Shift+R) بعد كل تحديث حتى تشوف النسخة الجديدة
2. **انتظر 1-2 دقيقة** بعد ما أقول "تم" حتى Vercel يخلص البناء
3. **دز screenshot** إذا صار خطأ وأنا أصلحه

## الفروقات بين الفروع:

- **`main`** ← اللي Vercel يستخدمه للنشر (اللي تشوفه)
- **`arena/019fd3da-...`** ← فرع العمل الخاص بي (ما يستخدمه Vercel)

**دائماً التعديلات تروح على `main` تلقائياً.**

## ملاحظات:

- Vercel Hobby plan: حد معين من الـ Deployments شهرياً (مجاني: 100 deployment/يوم)
- لا تحذف المشروع على Vercel
- لا تغيّر Environment Variables إلا إذا كنت تعرف شنو تسوي
